{
  "builds": [
    {
      "src": "ui/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "ui/storybook-static" }
    }
  ],
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/"
    }
  ]
}
